apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce # can be used by a SINGLE node
  resources:
    requests:
      storage: 2Gi # storage capacity of PVC
